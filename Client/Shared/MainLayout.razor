@inherits LayoutComponentBase

<MudThemeProvider/>
<MudDialogProvider/>
<MudSnackbarProvider/>

<MudLayout Class="mud-height-full">
    <MudAppBar Color="Color.Primary" Fixed="true">
        <MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="ToggleDrawer"/>
        @* <MudIcon Icon="@Icons.Material.Outlined.Signpost" Color="Color.Inherit"/> *@
        <MudButton Variant="Variant.Filled" Color="Color.Primary" DisableElevation="true" href="/">Petitions.xorg</MudButton>
        <MudSpacer/>
        <LoginDisplay/>
    </MudAppBar>
    <MudMainContent>
        @Body
    </MudMainContent>

    <MudDrawer @bind-Open="@open" DisableOverlay="true" Elevation="1" Variant="@DrawerVariant.Persistent"
               Color="Color.Primary" Class="mud-height-full" ClipMode="DrawerClipMode.Always">
        <NavMenu/>
    </MudDrawer>
</MudLayout>

@code
{
    bool open = true;

    void ToggleDrawer()
    {
        open = !open;
    }
}